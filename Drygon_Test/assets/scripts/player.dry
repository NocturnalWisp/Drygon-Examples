pub? external? cat = "objects/cat.yaml"

start():
    register_input("KEY", "UP", "DOWN")

    print(object.transform.pos)

    eat("Charlie", 67)
end


update(delta_time):
    object.transform.pos++
end

eat(name, amount):
    print(name + " eats " + amount + " strawberries.")
end
